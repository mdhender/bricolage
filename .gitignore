.idea/
.amp/portals/

# build output
deploy/linux/

# "go build ./cmd/earl" drops a binary here. Run commands with
# "go run ./cmd/<name>"; build one only when you want one.
/cmsd
/cmsdb
/earl

# local database directory; --db names a directory, the file inside is cms.db
var/

# local env overrides; these hold secrets (see internal/dotenv)
.env.local
.env.*.local
